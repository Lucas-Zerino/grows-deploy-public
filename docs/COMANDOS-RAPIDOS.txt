╔═══════════════════════════════════════════════════════════╗
║           🚀 COMANDOS RÁPIDOS - GROWHUB GATEWAY           ║
╚═══════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  RESTART (Use esses!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  make r                 → Reiniciar TUDO (mais rápido!)
  make restart           → Reiniciar todos os containers
  make restart-api       → Reiniciar só API (nginx + php-fpm)
  make restart-workers   → Reiniciar só workers
  make restart-db        → Reiniciar só PostgreSQL
  make restart-rabbitmq  → Reiniciar só RabbitMQ
  make restart-redis     → Reiniciar só Redis

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  LOGS & DEBUG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  make dev-logs          → Ver logs de TUDO (Ctrl+C para sair)
  make dev-logs-api      → Ver logs só da API
  make dev-logs-workers  → Ver logs só dos workers
  make dev-logs-webhooks → Ver só webhooks recebidos 🔔
  make ps                → Ver status dos containers

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  DESENVOLVIMENTO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  make dev-up            → Subir tudo
  make dev-down          → Parar tudo
  make dev-shell         → Entrar no container PHP
  make dev-shell-db      → Entrar no PostgreSQL
  make dev-rebuild       → Rebuild das imagens

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  OUTROS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  make help              → Ver TODOS os comandos disponíveis
  make clean             → Limpar logs
  make test-data         → Criar dados de teste

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  💡 DICAS RÁPIDAS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1. Mudou código PHP?        → make r
  2. Mudou worker?            → make restart-workers
  3. Problema com fila?       → make restart-rabbitmq
  4. Ver o que tá rolando?    → make dev-logs
  5. Container travou?        → make dev-down && make dev-up

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 Guia completo: RESTART-RAPIDO.md
🌐 API: http://localhost:8000
🐰 RabbitMQ: http://localhost:15672 (admin/admin123)

