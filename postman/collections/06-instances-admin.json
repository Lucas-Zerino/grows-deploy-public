{
	"info": {
		"_postman_id": "instances-superadmin-collection-2025",
		"name": "Administracao de Instancias",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
	},
	"item": [
		{
			"name": "Listar Todas as Instâncias",
			"event": [
				{
					"listen": "test",
					"script": {
						"exec": [
							"if (pm.response.code === 200) {",
							"    const response = pm.response.json();",
							"    if (response.data && response.data.length > 0) {",
							"        const firstInstance = response.data[0];",
							"        pm.environment.set('provider_id', firstInstance.provider_id);",
							"        pm.environment.set('external_instance_id', firstInstance.external_instance_id);",
							"        console.log('Saved provider_id:', firstInstance.provider_id);",
							"        console.log('Saved external_instance_id:', firstInstance.external_instance_id);",
							"    }",
							"}"
						],
						"type": "text/javascript"
					}
				}
			],
			"request": {
				"method": "GET",
				"header": [
					{
						"key": "Authorization",
						"value": "Bearer {{superadmin_token}}",
						"type": "text"
					}
				],
				"url": {
					"raw": "{{base_url}}/api/admin/instances",
					"host": ["{{base_url}}"],
					"path": ["api", "admin", "instances"]
				},
				"description": "Lista TODAS as instâncias do sistema (de todas as empresas e providers).\n\n**Auto-save:** Salva automaticamente o primeiro provider_id e external_instance_id encontrados para usar nas próximas rotas."
			}
		},
		{
			"name": "Desconectar Instância",
			"request": {
				"method": "POST",
				"header": [
					{
						"key": "Authorization",
						"value": "Bearer {{superadmin_token}}",
						"type": "text"
					}
				],
				"url": {
					"raw": "{{base_url}}/api/admin/instances/{{provider_id}}/{{external_instance_id}}/disconnect",
					"host": ["{{base_url}}"],
					"path": ["api", "admin", "instances", "{{provider_id}}", "{{external_instance_id}}", "disconnect"]
				},
				"description": "Desconecta instância diretamente no provider (WAHA/UAZAPI).\n\n**Parâmetros:**\n- `provider_id`: ID do provider (1, 2, etc)\n- `external_instance_id`: Nome/ID da instância no provider"
			}
		},
		{
			"name": "Deletar Instância",
			"request": {
				"method": "DELETE",
				"header": [
					{
						"key": "Authorization",
						"value": "Bearer {{superadmin_token}}",
						"type": "text"
					}
				],
				"url": {
					"raw": "{{base_url}}/api/admin/instances/{{provider_id}}/{{external_instance_id}}",
					"host": ["{{base_url}}"],
					"path": ["api", "admin", "instances", "{{provider_id}}", "{{external_instance_id}}"]
				},
				"description": "Deleta instância diretamente no provider (WAHA/UAZAPI).\n\n**Parâmetros:**\n- `provider_id`: ID do provider (1, 2, etc)\n- `external_instance_id`: Nome/ID da instância no provider"
			}
		}
	]
}

