-- Seed inicial do banco de dados
-- Este arquivo será executado automaticamente após o schema.sql

-- Nota: O superadmin será criado via script PHP usando as credenciais do .env
-- Este arquivo contém apenas dados auxiliares se necessário

-- Inserir tipos de mensagem suportados (exemplo de seed)
-- INSERT INTO supported_message_types (type_name) VALUES ('text'), ('image'), ('video'), ('audio'), ('document'), ('location'), ('contact');

